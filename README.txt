must have:
python 3.5+, pandas, matplotlib, sklearn, and their dependencies

to run:
python3 supervised.py

to get output from second dataset, change line 16 from:
TEST_SET = 'leaf'
to:
TEST_SET = 'bank'
